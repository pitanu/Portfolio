# Used by portfolio/api/contact.js. For local development copy this file to
# portfolio/.env and fill in real values. For the live site set the same
# variables in Vercel (Project -> Settings -> Environment Variables).
RESEND_API_KEY=
CONTACT_EMAIL=
